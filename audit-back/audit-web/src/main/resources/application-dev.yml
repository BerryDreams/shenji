spring:
  datasource:
    type: com.alibaba.druid.pool.DruidDataSource
    username: lwtczg
    password: lwtczg
    url: jdbc:mysql://baixx.site:3306/audit?useUnicode=true&characterEncoding=UTF-8&serverTimezone=Asia/Shanghai
  servlet:
    multipart:
      enabled: true
      max-file-size: 20MB
      max-request-size: 1000MB

mybatis:
  configuration:
    map-underscore-to-camel-case: true

server:
  port: 18080

logging:
  file:
    path: D:\\shenji

minio:
  endpoint: 10.2.9.173
  port: 8080
  accessKey: miniolwtczg
  secretKey: miniolwtczg
  secure: false
  bucketName: "audit"
  configDir: "/root/minio-data/"